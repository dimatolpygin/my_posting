# STATUS — текущее состояние проекта my-posting

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-08-26 (этап 0 закрыт, репозиторий на GitHub)
**Текущий этап**: нет активного. Следующий — Этап 1 «Канал: ВК → Одноклассники», в работу не взят.
**Следующий шаг**: ждём отмашки пользователя на этап 1. Критерии — в [`07_ROADMAP.md`](07_ROADMAP.md).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас my-posting | ✅ | `stage-0-done` | `87b53d3` | 2026-08-26 |
| 1 | Канал: ВК → Одноклассники | ☐ | `stage-1-done` | — | — |
| 2 | Ключи как источник тем | ☐ | `stage-2-done` | — | — |
| 3 | Факт-база | ☐ | `stage-3-done` | — | — |
| 4 | Промпт и сборка поста из фактов | ☐ | `stage-4-done` | — | — |
| 5 | Первые публикации потоком | ☐ | `stage-5-done` | — | — |
| 6 | Редиректор и учёт переходов | ☐ | `stage-6-done` | — | — |
| 7 | Деплой на okhost | ☐ | `stage-7-done` | — | — |
| 8 | Наблюдение и решение | ☐ | `stage-8-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

Нет. Этап 0 закрыт, этап 1 в работу не взят — ждём отмашки.

**Репозиторий**: `https://github.com/dimatolpygin/my_posting`, ветка `dev`.
Публичный, поэтому `отзывы.txt` из него исключён (ники заказчиков), `dostupi_my.txt`
и `.env` — тем более. Оба лежат только локально.

## Известные блокеры

| Что | Кого/чего ждём | Что нужно для разблокировки |
|---|---|---|
| Нет `OPENROUTER_API_KEY` | пользователя | завести ключ на openrouter.ai, положить в `.env` и `dostupi_my.txt`. Блокирует этап 4 |
| Нет `FIRECRAWL_API_KEY` | пользователя | ключ firecrawl (1000 запросов/мес бесплатно). Нужен `research.js`, блокирует этап 4 |
| Нет домена под панель и `PUBLIC_BASE_URL` | пользователя | домен с https. Без него postmypost не скачает обложку с прода. Блокирует этап 7, на этапе 1 обходится локальным туннелем |
| Даёт ли Kwork закрывающие документы юрлицам | пользователя | ответ «да/нет». От него зависит рекламный блок во всех постах и тема 10 в `темы.txt` |
| Вторая группа ОК не создана | пользователя | создать, подключить в postmypost, прислать `external_id`. На этапы 0–6 не влияет: работаем на существующей группе |
| Из контейнера не открываются openrouter.ai, api.kie.ai и registry.npmjs.org | решения по маршруту | с хоста они доступны, из контейнера — таймаут: у контейнера свой выход в интернет, мимо того, чем ходит хост. Сборка обойдена зеркалом (`NPM_REGISTRY`), но генерация текста и обложек локально работать не будет. Варианты: прокси в контейнер, либо этапы 4–5 гонять сразу на сервере. postmypost и firecrawl из контейнера доступны, публикация не задета |

## История закрытий

Записывать в обратном хронологическом порядке. Формат:
`YYYY-MM-DD — Этап N: <название> — tag stage-N-done — короткая заметка с конкретикой верификации`.

**2026-08-26 — Этап 0: Каркас my-posting — tag `stage-0-done` — коммит `87b53d3`**

Конвейер скопирован из `vkposter/delivery` в корень проекта (не в подпапку: так пути
из CLAUDE.md совпадают с реальными и workflow деплоя не нужен отдельный контекст сборки).
Чужой git-истории, `.env` и `node_modules` не тащили.

Проверено вживую: `app` и `postgres` оба `healthy`; `curl /health` → 200 с
`"service":"my-posting"` и `"применено":21` (последняя миграция `0021_drop_bot_wall_content`);
в БД `myposting` 14 таблиц, включая `sources`, `articles`, `posts`, `groups`, `prompts`;
`POST /login` под `admin` даёт 302 и cookie, `GET /` → 200, заголовок
«Обзор — Автопостинг в Одноклассники»; панель открыта человеком в браузере и подтверждена.

Два бага, найденных по ходу и починенных:
1. `pino-pretty` печатал время в UTC при контейнере в `Europe/Moscow` — не хватало
   префикса `SYS:` в `translateTime`. Разошлось бы с `post_at` и расписанием на три часа.
2. С этой машины контейнер не доходит до `registry.npmjs.org` (таймаут, хотя с хоста
   реестр открывается) — сборка падала на `npm install`. Реестр вынесен в build-arg
   `NPM_REGISTRY`, локально стоит зеркало npmmirror.

Не сделано намеренно: клиентские источники (`scama.net`, `all-comment`) остались
засеяны в применённых миграциях `0002`/`0003` — гасим отдельной миграцией на этапе 2,
а не правкой применённых файлов (иначе падение по контрольной сумме).

Перед первым пушем история переписана: `отзывы.txt` убран из обоих коммитов, чтобы
ники заказчиков не уехали в публичный репозиторий.

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и реально проверены.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит» (`git rev-parse --short HEAD`), «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `07_ROADMAP.md` и `CLAUDE.md`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.
